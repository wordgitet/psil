(begin
	(define (make-counter n)
		(lambda ()
			(set! n (+ n 1))
			n))

	(define (drive counter remaining last)
		(if (= remaining 0)
			last
			(drive counter (- remaining 1) (counter))))

	(define c (make-counter 0))
	(drive c 1000 0))
;; SPDX-License-Identifier: MPL-2.0
;;
